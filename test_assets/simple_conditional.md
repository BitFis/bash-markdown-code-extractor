# Simple conditional

Following ane example for some basic conditionals:

[//]: #conditional ([[ -f "[file]" ]])
```bash
echo [file] exists
```

[//]: #conditional (command -v "[command]" &> /dev/null)
```bash
echo [command] exists
...
